<!-- app/views/leg_equipments/leg.html.erb -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List of Leg Equipments</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f8f8f8;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
    }

    h1 {
      color: #333;
      text-align: center;
      margin-top: 20px;
      margin-bottom: 20px;
      font-size: 2em;
    }

    table {
      width: 80%;
      margin: 0 auto;
      border-collapse: collapse;
      background-color: #fff;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      overflow: hidden;
    }

    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    th {
      background-color: #007BFF;
      color: #fff;
    }

    tbody tr:hover {
      background-color: #f5f5f5;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      margin-bottom: 20px;
      padding: 10px 20px;
      background-color: #007BFF;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    a:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <h1>List of Leg Equipments</h1>
  <table border="1">
    <thead>
      <th>Name</th>
      <th>Specs</th>
      <th>Quantity</th>
      <th>Price</th>
      <th>Status</th>
    </thead>
    <tbody>
      <% if @all.present? %>
        <% @all.each do |leg_equipment| %>
          <tr>
            <td><%= leg_equipment.name %></td>
            <td><%= leg_equipment.specs %></td>
            <td><%= leg_equipment.quantity %></td>
            <td><%= leg_equipment.price %></td>
            <td><%= leg_equipment.status %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
  
  <a href="/equipmentlist">Go Back</a>
</body>
</html>
